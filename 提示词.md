# DeskPing 完整需求提示词

> 可直接复制粘贴给任何 AI 编程助手，用于从零实现或迭代本项目。
> 本项目当前已按此提示词实现完毕，见同目录源码。

---

请帮我做一个 Windows 桌面提醒器应用，名字叫 **DeskPing（一张纸提醒）**。

## 核心功能

1. **托盘常驻**：右下角系统托盘要有图标；双击图标显示/隐藏主窗口；**右键菜单必须包含"退出"**。
2. **三种计时模式**（窗口顶部 Tab 切换）：
   - **正计时**：从 00:00:00 开始累加，可开始/暂停/重置；
   - **倒计时**：可设置时长（时:分:秒输入框 + 快捷累加按钮 +5分/+15分/+30分），归零即提醒；
   - **目标计时**：设置目标时刻（今天/明天/自定义日期 + 时:分输入框），到点提醒；支持"每日重复"勾选（到点提醒后自动顺延一天，不打断运行）。
3. **到点提醒（必须醒目）**：
   - 主窗口自动弹出置顶，时间数字**上下跳动动画**，纸片边框**呼吸闪动**（转警示色）；
   - **任务栏图标闪烁**（Windows FlashWindowEx）；
   - 提示音（可关闭）+ 托盘气泡通知；
   - 用户点"知道了"按钮后停止动画、停止闪烁、重置计时。
4. **托盘右键菜单**：显示/隐藏纸片、模式切换（三选一勾选态）、主题（四选一勾选态）、深色模式、提醒音、开机自启、退出。
5. **设置持久化**：模式、时长、目标时刻、每日重复、主题、深色、提醒音、自启、窗口位置，保存到 `%APPDATA%\DeskPing\settings.json`，损坏时回退默认值绝不崩溃。
6. **关闭按钮（×）= 隐藏到托盘**，不退出；首次隐藏时托盘气泡提示"还在托盘待命"。
7. **单实例**：重复启动直接退出，避免双重提醒。

## 画风要求（重要，必须一致）

完整借鉴 PaperTodo 的"一张纸"设计语言：奶白纸张、圆角纸片、细描边、弱化文字、系统级阴影。**四套配色方案必须使用以下精确色值**（浅色版；深色版按同样风格加深背景、提亮文字）：

| 角色 | 暖纸 warm | 墨 ink | 林 forest | 霞 rose |
| :--- | :--- | :--- | :--- | :--- |
| 纸面背景 Paper | #FFF9EA | #F6F7F9 | #F3F8F1 | #FDF5F6 |
| 纸面描边 PaperBorder | #E0CEA7 | #D0D6DE | #C8DAC6 | #E4CDD2 |
| 正文 Text | #33291E | #262C36 | #26322A | #36262A |
| 弱化文字 WeakText | #8A7A63 | #767E8A | #6E8070 | #8C7278 |
| 强调 Active | #8C7350 | #5A6C86 | #588260 | #9E6876 |
| 内嵌底色 Code | #F7EDD2 | #ECEFF3 | #E9F2E7 | #F8ECEE |
| 链接 Link | #B06242 | #42689C | #3C8260 | #B2546E |
| 警示 Danger | #B05A46 | #BC5450 | #BC604C | #BC524E |
| 暖色叠加基 Tint | #785C30 | #465A78 | #466E50 | #965060 |

界面元素规范：
- 主窗口为**无边框圆角纸片**（圆角 12px，系统阴影，无自定义外发光），约 384×312；
- 标题行文字"DeskPing · 一张纸提醒"（正文色 + 弱化色），右侧 −/✕ 自绘按钮；
- 顶部三个 Tab 文字弱化色，选中项正文色 + 强调色圆头短下划线；
- 时间数字大号（约 40pt）居中，微软雅黑；
- 主按钮"开始/暂停"强调色填充圆角，重置为描边按钮，提醒时主按钮变警示色"知道了"；
- 输入框无边框 + 下划线（聚焦时变强调色加粗）；
- 状态行小字弱化色居中。

## 技术约束（功耗最低，硬性要求）

1. **Windows x64 原生应用**；推荐 .NET WinForms + GDI+ 自绘（比 WPF 常驻合成器更省电）；不要 Electron/WebView。
2. 计时核心独立于 UI：单个 **1Hz 节拍器**（System.Threading.Timer）做秒级比较，空闲 CPU ≈ 0%，禁止忙轮询。
3. 窗口隐藏时**零 UI 刷新**；所有绘制按需 Invalidate。
4. 提醒跳动动画仅提醒期间运行（约 25fps），确认后立即停止；FlashWindowEx 由系统处理，无需自己闪烁。
5. 图标运行时 GDI+ 绘制（纸片 + 钟面），不依赖 .ico 资源文件；及时 DestroyIcon 防泄漏。
6. 设置读写只在启动/退出/变更时进行，不常驻监听文件。

## 验收标准

- [ ] `dotnet build -c Release` 零错误零警告；
- [ ] 托盘双击显隐、右键可退出；
- [ ] 三种模式均能开始/暂停/重置；
- [ ] 倒计时归零与目标时刻到点均触发：弹出 + 跳动 + 边框呼吸 + 任务栏闪烁 + 声音 + 气泡；
- [ ] 每日重复到点后自动顺延不停止；
- [ ] 任务管理器观察：静置 1 分钟 CPU ≈ 0%，内存 < 60MB；
- [ ] 重启后设置与窗口位置恢复。
